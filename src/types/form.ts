export interface FormValues {
  name: string;
  email: string;
  phone: string;
  origem?: string;
  consumo?: string;
}

export interface CondominioInfo {
  nome: string;
  comercial: string;
  tipo: string;
} 