import React from 'react';
import LeadForm from '@/components/LeadForm';

const FlyerCondominios: React.FC = () => {
  return <LeadForm type="flyer_condominios" />;
};

export default FlyerCondominios; 